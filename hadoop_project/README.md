# Usage

Start Hadoop by using:
start-dfs.sh
start-yarn.sh 

Stop Hadoop by using:
stop-yarn.sh
stop-dfs.sh

Notes:
These commands should be run from the terminal, and you may need to ensure that the HADOOP_HOME/bin directory is in your PATH.